extends template

block content
  ul#files
    - files.forEach(function(obj) {
    li
      a(href='/download/#{obj.filename}') #{obj.originalname}
      button.deleteButton(type='button', data-filename='#{obj.filename}') 削除
    - });
